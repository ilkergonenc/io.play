<div class="typo-body grid grid-cols-1 md:grid-cols-2">

  <div data-skin='light' class="bg-skin-base p-12">
    <div class="w-1/2 mx-auto bg-surface text-on-surface p-4 rounded drop-shadow border border-skin-soft">
      <h5 class="heading mb-2">Foo Bar</h5>
      <h6 class="text-muted heading-sub mb-2">Foo Bar</h6>
      <p class="py-2 mb-2">We define colors based on the role they play in the interface. There are 10 color roles, which we use to generate the values of all the color variants in the palette.</p>
      <div class="flex justify-end">
        <button class="bg-primary text-secondary typo-button px-6 py-2 rounded drop-shadow">Explore</button>
      </div>
    </div>
  </div>

  <div data-skin='dark' class="bg-skin-base p-12">
    <div class="w-1/2 mx-auto skin-dark bg-surface bg-opacity-50 text-on-surface p-4 rounded drop-shadow border border-skin-soft">
      <h5 class="heading mb-2">Foo Bar</h5>
      <h6 class="text-muted heading-sub mb-2">Foo Bar</h6>
      <p class="py-2 mb-2">We define colors based on the role they play in the interface. There are 10 color roles, which we use to generate the values of all the color variants in the palette.</p>
      <div class="flex justify-end">
        <button class="bg-primary text-secondary typo-button px-6 py-2 rounded drop-shadow">Explore</button>
      </div>
    </div>
  </div>

  <div data-skin='lime-dark' class="bg-skin-base p-12">
    <div class="w-1/2 mx-auto skin-dark bg-surface bg-opacity-50 text-on-surface p-4 rounded drop-shadow border border-skin-soft">
      <h5 class="heading mb-2">Foo Bar</h5>
      <h6 class="text-muted heading-sub mb-2">Foo Bar</h6>
      <p class="py-2 mb-2">We define colors based on the role they play in the interface. There are 10 color roles, which we use to generate the values of all the color variants in the palette.</p>
      <div class="flex justify-end">
        <button class="bg-primary text-secondary typo-button px-6 py-2 rounded drop-shadow">Explore</button>
      </div>
    </div>
  </div>

  <div data-skin='lime-light' class="bg-skin-base p-12">
    <div class="w-1/2 mx-auto bg-surface text-on-surface p-4 rounded drop-shadow border border-skin-soft">
      <h5 class="heading mb-2">Foo Bar</h5>
      <h6 class="text-muted heading-sub mb-2">Foo Bar</h6>
      <p class="py-2 mb-2">We define colors based on the role they play in the interface. There are 10 color roles, which we use to generate the values of all the color variants in the palette.</p>
      <div class="flex justify-end">
        <button class="bg-primary text-secondary typo-button px-6 py-2 rounded drop-shadow">Explore</button>
      </div>
    </div>
  </div>

</div>

